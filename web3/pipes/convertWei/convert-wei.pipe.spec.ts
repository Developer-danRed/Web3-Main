import { ConvertWeiPipe } from './convert-wei.pipe';

describe('ConvertWeiPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertWeiPipe();
    expect(pipe).toBeTruthy();
  });
});
