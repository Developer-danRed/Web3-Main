import { IsValidPairPipe } from './is-valid-pair.pipe';

describe('IsValidPairPipe', () => {
  it('create an instance', () => {
    const pipe = new IsValidPairPipe();
    expect(pipe).toBeTruthy();
  });
});
